# 复制本文件为 .env 后填写真实值（.env 已被 git 忽略）
# cp .env.example .env

OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL_NAME=gpt-4o-mini

# 可选：8080 被占用时改为例如 8081（对应 application.yml 的 server.port）
# SERVER_PORT=8081
