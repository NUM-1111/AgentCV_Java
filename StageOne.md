# Stage One：核心功能与技术亮点

本文档描述 **Job Agent（Java 重构版）** 在当前阶段解决什么问题，以及相对早期 Python 手写 Prompt + 正则抠 JSON 的方案，在工程上做了哪些取舍与强化。

---

## 一、本阶段核心功能（解决了什么问题）

本阶段交付的是一套 **面向求职场景的 Spring Boot + LangChain4j 后端骨架**，把「岗位 JD → 结构化理解」「简历与 JD 匹配评估」「按 JD 重写项目经历」三条链路，收敛为 **可 HTTP 调用的稳定 AI 能力**，而不是一次性脚本或不可控的自然语言输出。

| 能力 | 接口 | 输入 / 输出要点 |
|------|------|------------------|
| **JD 解析** | `POST /api/v1/jd/analyze` | 原始 JD 文本 → 结构化 `JdInfo`（岗位名、职责、必备技能等） |
| **人岗匹配** | `POST /api/v1/match/evaluate` | JD 文本 + 简历纯文本 → `MatchReport`（分数、匹配/缺失技能、改进建议） |
| **简历项目重写** | `POST /api/v1/resume/rewrite` | JD + 原始项目描述 → `RewriteReport`（STAR 风格要点与优化说明） |

**解决的问题可以概括为：**

- 把招聘侧（JD）与候选人侧（简历/项目经历）放在 **同一套强类型契约** 下，便于前端或后续 Agent 编排直接消费结构化结果。
- 通过统一配置（如 `application.yml` 中的 OpenAI 兼容端点）管理模型访问，避免业务代码里散落 HTTP 细节。

---

## 二、技术亮点（按 AI 应用开发工程师的重要性，共两点）

以下两点侧重 **「先保证输出可依赖，再谈架构可替换与提示可组合」** —— 这也是 AI 应用岗面试中最常被追问的优先级。

### 亮点一：解决 LLM 输出格式幻觉与不稳定性（Record + `@Description` + JSON Schema 强约束）

**为什么排第一：** 应用层最大的风险往往不是「模型不够聪明」，而是 **输出不可解析、字段漂移、半句 JSON**。没有结构化输出，后面所有业务逻辑都是沙上建塔。

**本阶段做法：** 使用 **Java 17 `record`** 作为返回类型，并在字段上使用 LangChain4j 的 `@Description` 做 **字段级语义约束**；框架侧会将该结构映射为 **JSON Schema**，在请求链路中约束模型走 **Structured Output** 路径，使模型从「写作文」转为「填表」。

**面试话术（可背）：**  
「我没有靠 Prompt 里写『请输出 JSON』然后正则抠字符串。我用 Java Record 加 `@Description`，让框架在请求里带上严格的 JSON Schema，把大模型输出锁死在强类型对象上，从根上减少反序列化失败和字段幻觉。」

---

### 亮点二：声明式 AI 调用与多参上下文注入（`@AiService` + 动态代理，`@V` + Mustache 模板）

**为什么排第二：** 在输出已被 Schema 兜住的前提下，下一层关键是 **调用方式是否工业级** —— 既不把 HTTP 细节泄漏进业务，又能在 JD 与简历匹配、按 JD 重写项目等场景里 **一次请求注入多段长文本**，避免手写拼接与重复 Prompt 代码。

**本阶段做法：**

- **`@AiService`：** 在接口上声明 System / User 模板与返回类型，由 Spring 与 LangChain4j 生成代理，完成 **Prompt 组装、变量替换、请求发送**；业务与具体模型服务商、端点配置解耦，换 OpenAI 或本地 Qwen 等兼容实现时，业务方法可保持不变，主要改配置与 Bean。
- **`@V` + 模板：** 在 `@UserMessage` 中使用 `{{jdText}}`、`{{resumeText}}` 等占位符，方法参数用 `@V("...")` 与占位符名绑定，单次调用内精准注入复杂长文本，模板与参数关系清晰。

**面试话术（可背）：**  
「我没有在业务里裸调 HTTP Client，而是用 `@AiService` 结合动态代理，在接口层定义业务语义，框架底层完成协议细节。做多维度对比时，再用 `@V` 把 JD、简历等上下文绑定进 Mustache 风格模板，一个请求周期内注入完整输入，代码干净、可维护。」

---

## 三、与早期 Python 方案的对比（一句话）

早期常见路径是：**大量 Prompt 约定格式 → `utils/parser.py` 正则抠 JSON → 处处 try-catch 防半个逗号**；本阶段用 **Schema 强约束 + 声明式 AI 接口 + 模板变量绑定**，把不确定性从「业务代码里消化」前移到「框架与模型协议层消化」，更符合 **工业级 AI 应用** 的分层方式。

---

*文档对应仓库阶段：Spring Boot 3.2 + Java 17 + LangChain4j 0.36.x，OpenAI 兼容 Chat API 配置。*
