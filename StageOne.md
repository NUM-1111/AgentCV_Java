# Job Agent MVP 4.0 进阶演进指南 (Agentic Workflow 优先版)

## 演进背景与路线修正
在 MVP 3.0 中，我们通过 Facade 模式、宽容解析与 JSR-380 建立起了单步调用的防御体系。
经过架构评审，鉴于当前系统处于“无数据库（Stateless）”状态，强行上马全局异步（Async/MQ）会引入复杂的本地状态管理风险，且偏离了 AI 应用开发的核心考点。
**因此，路线修正为：优先在单次 HTTP 请求生命周期内，实现复杂的多智能体协作（Multi-Agent），彻底解决大模型的“幻觉”痛点。**

---

## P0：消灭幻觉 —— Actor-Critic 双智能体审查流 (External Feedback)

**目标**：实现“事实边界控制”，确保大模型重写的简历绝对基于原始素材，拒绝无中生有。

* **业务痛点**：单步模型为了匹配高阶 JD，极易产生“发明数据（如乱写QPS）”和“捏造技能（如强行写精通K8s）”的幻觉。
* **架构设计**：
    1. **`ResumeWriterAgent` (执行者)**：接收 JD 和原简历，负责扩写和包装。
    2. **`FactCriticAgent` (审查者)**：接收原简历和 Writer 的草稿，做交叉比对（Cross-check），专门挑刺。
    3. **`Coordinator` (协调层)**：普通的 Java 方法，负责串联两者。收到挑刺结果后，打回给 Writer 重新生成。最大循环次数设为 3（防止死循环）。
* **技术收益**：用纯工程的逻辑循环，兜底 AI 的概率性幻觉问题，直接拉满 AI 研发含金量。

---

## P1：动态工具调用能力 (Tool Calling)

**目标**：打破模型预训练知识的隔离，赋予系统主动探索能力。

* **业务痛点**：简历重写不能只靠“改写词汇”，遇到模型不熟悉的垂直领域技术栈，包装效果极差。
* **架构设计**：
    * 为 Agent 挂载外部工具（例如本地的一个 `TechStackKnowledgeBase` 类，或者模拟的搜索 API）。
    * 利用 LangChain4j 的 `@Tool` 特性，让大模型在重写前，自主决定是否需要先“查阅”技术名词的真实应用场景。

---

## P2：白盒化质量评测 (Eval-Driven)

**目标**：用数据量化重构带来的收益。

* **业务痛点**：没有数据支撑，无法向业务方（或面试官）证明双智能体架构的优越性。
* **架构设计**：
    * 构建一个小型的“黄金测试集（Golden Dataset）”。
    * 引入 LLM-as-a-Judge，分别跑一次“单步生成”和“双智能体生成”，对比**幻觉率**。

---

## 🛑 架构权衡与待决议项 (Trade-offs & Open Questions)
*在实际开发前，必须明确以下工程问题：*
1. **上下文爆炸问题**：在 Actor-Critic 循环中，打回重写时，要不要把之前的“错误草稿”也作为历史上下文传给大模型？
2. **响应时间 (RT) 劣化**：虽然暂缓了异步化，但如果最多循环 3 次，加上最初的解析，一个 HTTP 请求可能长达 40 秒以上，网关层/前端大概率会报 Timeout，怎么缓解这一阵痛？