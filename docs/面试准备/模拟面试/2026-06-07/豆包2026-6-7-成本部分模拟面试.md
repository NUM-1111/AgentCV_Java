这个方法可行。比如针对简历改写场景，你觉得可以用传统方法提取哪些关键信息，来替代把整份简历都喂给模型？
比如说对于一个简历的话，我可能会从一些关键信息，学校啊、专业，然后包括是擅长的一些技术，然后然后是项目，项目内容，项目内容中然后根据从第一点的一个提到基础上开始，一直到后面去做瘦身，然后包括实习的经历等等。然其实这样的关键字它会去做一个字符串的一个截断。
嗯，提取这些关键信息后，输入 token 量应该能减少不少。那如果预处理后输入 token 还是降不下来，你会考虑调整模型的哪些参数来控制成本？
这个就涉及到我们的一个业务了，你比方说，对于我们的 agent， 就我可能直接进行一些调参来控制我们的一个输出也是可能的。你比方说这个 write a 政策的话，就是通主导一个扩写的这样一个 a 政策，我可能会选择，将它的一个 temperature 调的更高一点，然后让它扩写的更加生动有效率一点。然后然后比如那如果说是 created， 我可能会把它的 temperature 调低一点，因为它只要和我们的这样一个评判的一个维度去做一个，这一个机械化的一个事情就可以了。然后相比于改写来说呢，更加的固化它的一个流程，那这样也可以有效的减少我们这样一个 token， 对，就大概可能是通过一些调参的手段来改变一些模型的输出，从而减少我们的这样一个这个成本。
其实 temperature 主要影响输出随机性，对 token 量的直接影响不大。更直接的参数比如 max_tokens，你会考虑怎么用它来控制输出成本？
这个可能我这个项目还要暂时处理一下吧，如果是我的话， nice talk。这个可能我不太熟悉。对，我后续还会经历你的。
没关系，这个可以后续再深入。那除了输入优化和模型选择，你觉得还有哪些长期的成本控制策略可以考虑？
先暂时到这里把，本次模拟面试我觉得挺好，然后可以暂时告一段落了。
好的，这次模拟面试就到这里。你在成本控制的思路上比较清晰，尤其是输入预处理和模型匹配这两块考虑得很具体，后续可以再补充一些参数调优和长期策略的内容，会更完整。



