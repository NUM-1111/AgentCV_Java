好的，面试开始，现在开始提问。
你提到了项目里做了 Token 预估，那我先问第一个问题：Transformer 模型里的 Token 到底是什么概念？它是怎么切分出来的，用到了什么算法？另外中文场景下的 Token 你又是怎么估算的？
喂，你好，可以说话，可以听到。
好的，那你先完整说一下 BPE 算法的基本流程吧。
它其实主要是做一个，嗯， token， 关于 token 一个切分，或者根据频率去把它组合在一起的一种策略，或者我们可以称之为算法。那具体是怎么回事呢？它主要在我们的模型的两个阶段做一个发挥它的作用。第一个是它的这个推推训练的时候，阶段训练的时候呢，我们根据不同的一个 EPE 的一个算法，我们会把它从这个文字切，就是切换到我们的 token 的一个形式，对每个去贴上不同的标签，形成一部字典。那么在训练的时候主要是做的这样一件事情，比如说人工智能，人工和智能可能是作为一个 token 的一个编号去存储，然后或者是人工智能本身也可以作为一个编号去存储，这是它的第一步及训练的时候发挥的作用。第二步呢，主要是在我们模型进行推理的时候的 BPE 会根据它的一个算法内容，在这个去找到我们在字典里间频率最高的一部分。你比方说当前这个输出这个内内容是人工智能，它就会直接找到这个切片，但如果说是没有的话，它就会根据上一轮拼接，比如说人和工，然后拼接之后，在下一轮拼接的一个频率更高的一个这个 token 去找到他，然后再切到后面，然后完成完整的一个推理，直到我们达到了一开始我们想要的这个 token 的一个总数量，然后主要是做这样的一个事情。
好，那接着问第二个必答题：你用的 DeepSeek V4-Flash，它的上下文窗口是多大？为什么你在项目里设置 1 万 Token 的限制，而不是更大或者更小？
V4 plus， 在 ToC 新推出的一个上下文窗口中呢，它的一个最大限度的 token 量，对，包括它的 Pro， 包括 V4 plus， 对，都是这样一个上下文的一个量级。那在我的这样一个项目中呢，我设置的我的一个 API 的一个上下文的一个限制为 1 万，为什么值呢？首先是因为，一个就是我们这个模型它是有一个现象叫做 lost in the media， 也就是说这个大模型天生会关注在这个头部和尾部大概 25% 左右的一个信息量。关于中间的一部分的通常会选择说 100 上下文，如果我们设置为也是设置接近的一个数值的话，很可能会导致我们中间的一部分内容，它是没有起到很好的一个模型推理的一个效果，对，这是第一点就是根据我们的一个模型效果去说，第二点是根据我们的一个成本去考虑的。因为首先如果我们去设置一个较大的一个 token 量，对吧？那个上下文窗口的限制话，那么即使我们保证它是在，嗯，比如说六十四千这样一个范围，可能也不会产生这个 loss in the middle 这样一个现象，但实际上它相比于 1 万就要大很多，在实际上我们产生费用，尤其是我这个模型的调用，它是进行了一个三轮的这样一个调用，对吧，每一次调用包括 right 检测，包括剔离 agent 都需要经历这样一个这么多的上下文，假设我们的一个上下文窗口设置这么大的话，且我们的输入的一个 token， 我们要处理的一个 token 也这么多的话，那么到时候加起来的成本，虽然说 diff 它本身一个这个这个成本很低，对吧？但是我们还是得要尽可能去削减这样一个成本。第三个就是它的实际的没用说呢，就是说对于我们这个核心业务而言，直接上，我们真实的一个简历的话，一个中文的一个 gd， 大概也就是最多可能详细一点就是五百多字，那可能简单一点就是两百多字，对吧？那么简历的话，假设一些比较极端的写的特别特别多的简历，可能最最多也就是一千五百多字，那么换算成那可能也就是大概两千多，对吧？加上一些固定的一些思考或者上下文的一些开销而言，对吧？那么这样的话，我们如果设置一个过高的一个上下文就显得非常不合理，对吧？那一些用户可能输出一些别的东西的话，那纯纯是在浪费我们的一个模型的一个，那这也是这一方面的一个考虑，就是说正常的一个简历不会是具有如此大的一个文本量的，所以这也是我们设置一万 token 的一个理由，那至于为什么设置成一万 token， 那为什么不设置多一点或者少一点，那我的意思是比如说设置为六千呢，因为我刚才不是说他可能是最多就是三千个中文三千个，那我为什么不设置成六前呢，可能多一点，其实我也是一个大概保守的一个估计值罢了啊，通过，但是后期的话，其实真正的一个实际的一个线上开发的一个流程来说，我们实际上是应该在线上去大概跑一个月这样的一个数据，然后最后再通过模型的一些成本的一些估算，一些一系列的一个工程化的一个行为呢？去得到我们最终这样一个上下文设置为多少才是最合理的。这里的话我就是做一个保守估计，当然这样一个估计对我们这样一个项目来说，它其实已经达到了一个非常好的一个效果，对。
先纠正一下，DeepSeek V4-Flash 上下文是 1M Token。回到问题，你刚才提到了 Lost in the Middle，那接着问第三个必答题：既然模型支持 1M 上下文，你有测试过上下文填到百分之多少的时候回答质量会下降吗？结合你说的 Lost in the Middle 具体展开说说。
实际上我们也通过一些文本的一个测试，大家去做了这样一个判断，那大概在一个，可能是 64 或者到 80K 这样的一个范围之后呢，就出现了一些这样或者说处理，我们可以有最后的一个减少这样一个问题，对吧？当然我的一个检测图片，但是实际上就相关的研究论文啊，它不是一个大，就是说百分比的一个问题，它是基本上目前的一个模型呢，它的一个就是说我们的一个上下文的一个发生 lost in the middle 的一个，强制的一个限制量的 token 呢，大概是在 64K， 大概就是这样一个范围内，对，所以一但超出这个范围呢，我们的一个文本的一个大模型的推理质量它就可能会下降。
现在深挖追问 A：你说设 1 万 Token 是保守估计，但这个数字具体有什么科学依据？为什么不是 8000 或 12000？你刚才提到留了余量，设 2-3 倍余量在工程上到底有什么讲究？
这里设置一辆，我刚才已经说过了，1 万是根据我们通过推算，我们最极端的简历和最极端的一个 GD， 也就是岗位匹配去做出的一个推论，比如站在这个推论上去设量两三倍的这样一个余额，然后最后定到了我们这样一个 1 万 token 的上下文的一个限制，那么你至于说为什么不是 8,000，为什么不是 12,000，那不好意思，那我实际上在设置的时候呢，只觉得 2 到 3 万它是一个更加安全合理的一个部分，因为实际上来说的话，我们谁谁实实谁实际上来说，真正你要定去定死一个特别最优化的一个成本的一个界限的上下文的一个窗口，一个限制呢，那你肯定是要去经过线上跑数据，去经过数据的一些分析和检测，然后最后总共才定下来，我们是要确定是这个东西。那么在我们这样一个项目阶段呢，没有的一个大量的一个用户数据，数据流量很少，那么我们重点的是采取相对保守的一个策略，设置它的余量为 2 到 3 倍，你最多非要有多科学，它没有多科学，就是根据我们的一个一个红线的一个点，往后再给它多延了几倍而已，看我也说了，如果你真的想要去定下来的话，那我们可以去跑一个几个几个月的一个数据，去让这个事情真正落落实落点下来，确定这个真正的一个上下文文创能够量应该是为多少。
接着追问 B：模型明明支持 1M 上下文，你把 1 万 Token 丢进去根本不会崩，现在主动设这个拦截阈值，是不是有点故意制造困难再解决？说到底这个 1 万的拦截到底实际解决了什么核心问题，而不是单纯为了控成本？
对，对于模型而言的话，模型它的上下文是有 100 万，也就是 1M 的一个 token 能量，它的上下文限制确实是这样，即使是我们所说的 nothing in the media 的这样一个情况，想要我们的一个管控的一个信息的一个质量是最高的一个情况下呢，即使这样，大概也能在我们所设定的一个上下文窗口的大概 7 到 8 倍的一个这样一个样子。但是至于为什么要设计成这样，到底是不是真正知道困难，其实我想说的是，我们实际上预估这样的一个 1 万的一个拦截阈值呢，不是为了去真正的去说这个模型根据这个技术问题去设定啊，说我明明可以拦截啊，可可以处理的有这么多，我只设这么多，而是结合我们真正的一个，你有个场景来看的话，你没有人，就没有一个正常人会把简历写到大概有七八千字到 1 万字，没有这样的一个东西，我们适合于普遍的一个大大部分的一个客户呢，所使用的这样一个上下文的量在 1 万之间就已经足以满足了。但实际上是为了我们的业务而去界定，不是因为技术而去界定的，这是第一点，你想说它实际上解决了什么核心问题，就像我刚才所说的，对吧，就是嗯把一些就是不是正常搞搞一些输入的一些用用户的一些习惯，或者一些不好的一些非法的一些异常输入都给他刨开，这样的话，就第一个是让我们的一个 token 成本最主要的一个问题就是在这里，嗯，那么我刚才已经说过了。
现在追问 C：如果遇到一份简历格式极不规范，连 “工作经历” 这种正则关键词完全匹配不到，你这套基于关键词切分 + Token 预估的流程就卡壳了，这种情况你设计了几层兜底方案？具体怎么处理？
我想问有几条兜底方案，是吧？其实这里的话，首先对于内容极少的简历，我们实际上会搜寻和所有有效的一个信息，具体有几层呢？好吧，这就是一个点，对吧？这这里我大概的话，我想一下，我脑子里还没有形成一个比较聪明的一个比较结构的一个系统的回答，我只能根据我脑子中你有哪些数据去说。具体的一个处理方式就是说我们根据我们 GH 获取提前预处理下来的一个压缩文本，得到一个内容呢？显示如果说这个个我们最终的一个收入的一个 token 量，它可能在 100 内就是大概 50 50 token， 就这么一点内容的话，而且基本都是无关信息，我们就直接告诉用户，这个简历它实际上已经完全是缺少各种的核心的一个内容或者信息的这样一个东西。那至于我们实际上没有完成的东西呢？
接着追问 D：你刚才说 Token 预估是自己做的保守算法，为什么不用成熟的 tiktoken 库直接计算？自己写的预估模型误差有多大，保守策略下会多算多少 Token，这对实际业务的成本和拦截判断有什么影响？
就是说，实际上我。
先把这个问题说完，你选择自己写预估算法而不用 tiktoken，核心考量是什么？还有你这套保守预估的误差范围具体能到多少？
Token 预估是自己做的一个保守算法，这是没错的，因为 you think 它没有做一个官方文档的任何一个关键的一个说明书。嗯，这样一个 token 的处理的一些对应的 Java 的 GDP 内容，所以我还是做一个自己的一个策略计算。对于实际的一个 token 量来说，相差可能大概就在 10% 左右用，而且且不影响我们最后 correct 的一个结论的一个评价吧？对，嗯，大概是这样，嗯，好，这个跳出我作为一个面试者并回答来说，在这个地方肯定是准备不足的，我觉得肯定是准备不足的，对，这，这里是一个点，然后你标记一下，注意它就行。我们下一个问题。
那最后追问 E：你怎么验证这套基于 1 万 Token 限制的简历优化效果？有没有设计量化指标，比如优化后简历的关键词匹配度提升了多少，或者模型输出的有效信息占比有没有数据支撑？
OK， 这个问题我说要点出来，这问题我最后就是要点出来的，就是关于我们的一个， right 的这个，那就像平写的一个所谓的平衡标准，在我们这里仍然是以模型去预估的，这个模型预估它本质上是一个不准确的一个东西，包括最终的一个匹配评分也是模型进行评估和预估的，全程由模型进行操量的话，我觉得有时偏颇，有时 TikToken 作为一个审计，它只是审计最终是是不是有假数据是不是有问题，这只是指挥这些内容，对吧？那最终我们是不是达成了我们想要的一个精准匹配，我们 GD 的一个效果，我觉得我的算力是没有真正体现出来的，对吧？我第一次面试的时候，我就觉得这个点很混乱啊，前面面试官也没有声问，对吧？但是我觉得这绝对是一个问题点，就是说你怎么能，你怎么就觉得这个模型觉得，哎，我就是觉得你这个 GD 的一个评分高，它就是高了，你没有一个外在指标的呀，你没有觉得说，哎，比如说用一些什么诡异的一些向量化的一些东西去做一个匹配，你没有做这样的一个事情，对吧，那其实，嗯，你可以参考一下，就是说真正的一些互联网公司做一些简历触杀的时候，他们大概会进行什么样的出差，那你怎么样以史为终的去想到我们如何去做一些，就是让这个扩写的这个简历尽可能能通过这次出差呢，对吧，我们应该对对对。这个才是一个核心的一个要点那。
结合你说的模型自评估偏误，其实可以用 SBERT 做简历和 JD 的语义嵌入
回到现在的话，我觉得我们其实接下来可能要有一个小小阶段的一个开发，但是没有关系，我们有 AI 的话，这个开发的过程应该不会很困难。我们最重点的就是说去把这个 write 的一个包括评分，就是 write 之后它评分的达达到什么样的一个状态，或者说有什么样的一个依据，是不是真正能通这样的一个简历，这样一个东西，至少我们去做一个落地的验证，嗯，起码不落地的话，我们至至少自己要有一个验证，自己心里有一个底。说啊，你确实一个核心的一个需求就是帮助用户去改写简历，然后通过我们，对不同的一个 GD 的一个简历的一个匹配度，对不对？那这是我的一个所思所想，可以，就先到这里吧。那还有其他问题吗？
刚才聊到的外部验证思路其实很关键，那回到你这套 1 万 Token 拦截的落地，在没有大规模线上数据前，你有没有设计小范围的灰度测试方案，来同时验证拦截有效性和业务匹配度的平衡？
其实它也是没有的， OK， 我觉得问这么多问题，然后我们所暴露的点也显而易见了。这个东西，不需要你总结，我们到这里就 OK 了，好吧？然后这个东西我会待会交给 client， 然后让他帮我去整理一下。
好的，那后续如果在梳理过程中需要补充某个环节的细节，随时可以再沟通。