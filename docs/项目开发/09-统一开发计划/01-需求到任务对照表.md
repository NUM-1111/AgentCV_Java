# 01 — 需求到任务对照表

> **原则**：以 `08-产品全景分析/01-产品需求说明书.md`（2026-07-01）为唯一需求来源。
> 旧方案 `复习计划5-三层改进方案/01-项目设计与数据验证.md`（2026-06-19）中的条目，
> 逐条对照后分为三类：✅ 已实施 / 🔄 保留并调整 / ❌ 已推翻或降级。

---

## 一、新需求 → 开发任务

以《产品需求说明书》为驱动，拆解出以下开发任务：

### 功能需求

| # | 需求 | 开发任务 | 文件变更 | 优先级 |
|---|------|---------|---------|--------|
| F1 | 多维评分系统 | 改造 `ResumeScoringAgent`，从单一 matchScore 扩展为三维评分（JD匹配40% + 内容质量35% + 格式规范25%） | ResumeScoringAgent.java, OptimizationReport.java, 前端 | 🔴 P0 |
| F2 | 完整简历输入 | Controller 放开 3000 字符限制 → 10000；前端文本框改名"完整简历" | ResumeController.java, index.html | 🔴 P0 |
| F3 | 完整简历输出（回填） | 新增 `ResumeParser`（正则分段）→ 新增 `optimizeFullResume()` → `OptimizationReport` 新增 `finalResumeText` | 新建 ResumeParser.java, 改 ResumeOptimizationService.java, OptimizationReport.java | 🔴 P0 |
| F4 | 文件上传 | Controller 新增 `MultipartFile` 入口，引入 Apache Tika / PDFBox / POI 做文件解析 | ResumeController.java, 新建 DocumentParser.java | 🟠 P1 |
| F5 | SSE 进度条（简化版） | 降级 SSE 事件粒度：只发阶段级进度（评分/改写/审查），不发逐 bullet | ResumeOptimizationService.java, ResumeController.java, index.html | 🟠 P1 |
| F6 | 导出 MD | 前端生成 .md 文件下载 | index.html | 🟠 P1 |
| F7 | 导出 PDF | 后端生成 PDF | 新建 PdfExporter.java | 🟡 P2 |
| F8 | 复制完整简历 | 前端 `navigator.clipboard.writeText()` | index.html | 🟠 P1 |
| F9 | 评分缓存 | 快速评分后缓存结果，完整优化时复用 | ResumeOptimizationService.java | 🟠 P1 |
| F10 | 审查结论简化 | `OptimizationReport` 新增 `isReliable: Boolean`，隐藏 violations 细节 | OptimizationReport.java, 前端 | 🟡 P2 |

### 非功能需求

| # | 需求 | 开发任务 | 优先级 |
|---|------|---------|--------|
| NF1 | Token 消耗监控日志 | 在日志中记录每次 LLM 调用的 token 消耗（若 API 返回） | 🟡 P2 |
| NF2 | 评分权重可配置 | 当前写死 40/35/25，后续可改为 `application.yml` 配置 | 🟡 P2（不做，仅预留） |

---

## 二、旧方案对照：保留 / 推翻 / 已实施

旧方案来自 `复习计划5/01-项目设计与数据验证.md`（共 12 条 I-1 ~ I-12）。

| 旧条目 | 旧内容 | 新判定 | 说明 |
|--------|-------|--------|------|
| I-1 效果验证量化体系 | 维度分+相似度分+覆盖率分 | 🔄 保留，调整范围 | 与新 F1 多维评分重叠。旧方案偏"事后验证"，新方案把它前置为**评分引擎**（驱动改写） |
| I-2 RAG Chunking | IntelliVault 的 chunking 策略 | 🔄 保留，降级为 P2 | 属于项目二（IntelliVault），不影响 AgentCV 核心功能 |
| I-3 成本基准数据 | 10 份简历 token 测算 | 🔄 保留，降级为 NF1 | 属于辅助数据，不是功能 |
| I-4 Token 阈值 10K | 数据文档化 + 校准测试 | ✅ 已实施 | 无需再动 |
| I-5 通过率定义 | 87.5% 的定义澄清 | ❌ 推翻 | 新方案中审查结论不暴露给用户，通过率不再是对外指标 |
| I-6 产品合规免责 | disclaimer 字段 | 🟡 保留，P2 | 不影响核心功能 |
| I-7 MongoDB 深入 | IntelliVault 的存储细节 | 🔄 保留，降级为 P2 | 属于项目二 |
| I-8 Tool Calling | FactCheckTool + @Tool | ✅ 已实施 | FactCheckTool.java 已存在，Critic 已注册 |
| I-9 PDF/DOCX 解析 | Apache Tika | 🔄 保留，合并到 F4 | 与 F4 完全重叠，改选 PDFBox + POI（更轻量） |
| I-10 TokenEstimator 策略化 | 接口抽象 + 可切换 | 🔄 保留，降级为 NF2 | 属于优化，不是核心功能阻塞 |
| I-11 SSE 流式输出 | SseEmitter + 逐轮推送 | 🔄 保留，**调整为 F5** | 旧方案是逐轮推送详细事件，新方案简化为**进度条**（阶段级），不再推送逐 bullet |
| I-12 RAG 集成 | AgentCV+IntelliVault 打通 | 🔄 保留，降级为 P2 | 属于跨项目整合，不影响 AgentCV 独立可用 |

---

## 三、最终开发优先级（一页纸）

```
🔴 P0 — 核心功能阻塞（必须先做）
  F1  多维评分系统（JD匹配+内容质量+格式规范）
  F2  完整简历输入（放开字符限制）
  F3  完整简历输出（ResumeParser + 回填 + finalResumeText）

🟠 P1 — 产品体验完整度（紧接着做）
  F4  文件上传（PDF/MD/Word）
  F5  SSE 进度条（阶段级推送，前端进度条 UI）
  F8  复制完整简历
  F6  导出 MD
  F9  评分缓存

🟡 P2 — 锦上添花（有时间做）
  F7  导出 PDF
  F10 审查结论简化
  NF1 Token 消耗监控
  I-2/I-7（项目二相关）
  I-3 成本数据测算
  I-10 TokenEstimator 策略化
```

---

## 四、下一步

按四步法：

1. **用户故事** — 已在《产品需求说明书》第 2 节 ✅
2. **数据流** — 已在《产品需求说明书》第 5 节 ✅
3. **任务拆解** — 本文件第 1 节 = 待细化为可独立验证的小任务
4. **逐个实现** — 下一步：把 F1/F2/F3 各拆成 1-2 个小任务，开始写代码