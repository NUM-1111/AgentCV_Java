# 串行性能优化设计（placeholder）

> **状态**：待讨论
> **触发**：optimizeFull 串行处理多个项目，加上 ResumeParser 分段问题放大后，导致请求超时
> **关联**：`ResumeOptimizationService.optimizeFullResume()` L149-L161

---

## 当前问题

- 多项目串行 `runActorCritic()`，单项目 30-90s，N 个项目 = N × 时间
- DeepSeek API 有 RPM 限制（免费版尤其严格），直接并发可能触发限流

## 当前设计（有意识的选择）

从总驾驶舱 L2 决策层（`00-项目拆解方法论-总驾驶舱.md` L4 决策 7）：
> "串行不是不会做并发——是主动选择。LLM API 有 QPS 限制，MVP 阶段先验证核心链路的正确性，并发放在 Phase 2。"

## 待讨论方向

- [ ] ResumeParser 分段修复后，真实项目数从 9 个降为 2 个，单次请求时间是否在可接受范围
- [ ] 并发控制策略：`CompletableFuture` + `Semaphore` 限制并发度为 2-3（避免触达 DeepSeek RPM 限制）
- [ ] SSE 流式推送各项目进度是否已足够让用户感知进度（而非真正需要缩短总时间）
- [ ] 是否需要先修好分段、端到端跑通后，再决定是否做并发优化

---

> **创建时间**：2026-07-18
> **关联文档**：`01-行动闭环计划-开发与面试双螺旋.md`